name: Set of tests for Self-Hosted RISC-V runner

on: [workflow_dispatch, pull_request]

jobs:
  run:
    runs-on: self-hosted

    steps:
    - name: CPU info
      run: |
        uname -m
        cat /proc/cpuinfo
