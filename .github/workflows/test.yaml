name: Set of tests for Self-Hosted RISC-V runner

on: [workflow_dispatch]

jobs:
  run:
    runs-on: self-hosted

    steps:
    - name: CPU info
      run: |
        uname -m
        cat /proc/cpuinfo

    - name: OS info
      run: cat /etc/os-release
